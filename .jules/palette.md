## 2026-05-01 - Added ARIA labels and focus states to Gallery Lightbox buttons
**Learning:** Icon-only buttons in modals (like lightboxes) often lack semantic context for screen readers and visible focus indicators for keyboard users.
**Action:** Always add descriptive `aria-label` attributes and visible focus states (e.g., using `focus-visible:` utility classes) to interactive icon-only elements.
