## 2024-04-16 - Add ARIA Labels to Icon-Only Buttons
**Learning:** Found several icon-only buttons (like chatbot toggles, video play/close buttons, message send buttons) missing `aria-label` attributes.
**Action:** Adding `aria-label` to these buttons improves accessibility for screen reader users by describing the button's action.
