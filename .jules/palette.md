## 2024-05-24 - Missing ARIA labels in icon-only buttons
**Learning:** Icon-only buttons (like chatbot toggle and send button) often lack `aria-label` attributes, making them inaccessible to screen readers.
**Action:** Always add descriptive `aria-label`s to icon-only buttons to ensure they can be understood by assistive technologies.
